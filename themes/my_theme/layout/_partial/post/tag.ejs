<% if (post.tags && post.tags.length){ %>
  <ul class="article-tag-list">
    <% post.tags.forEach(tag => { %>
      <li class="article-tag-list-item"><a href="<%- url_for(tag.path) %>">#<%= tag.name %></a></li>
    <% }); %>
  </ul>
<% } %>